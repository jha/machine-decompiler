language: cpp

os:
  - linux
  - osx
  - windows

sudo: false

script:
  - mkdir build && cd build
  - |
    if [[ "$TRAVIS_OS_NAME" == "windows" ]]; then
      cmake -DCMAKE_GENERATOR_PLATFORM=x64 ..
    else
      cmake ..
    fi
  - cmake --build .